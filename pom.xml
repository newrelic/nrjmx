<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>nrjmx</groupId>
  <artifactId>nrjmx</artifactId>
  <version>1.0.4</version>
  <name>nrjmx</name>
  <description>The New Relic JMX tool provides a command line tool to connect to a JMX server and retrieve the MBeans it exposes.</description>
  <organization>
    <name>New Relic, Inc.</name>
    <url>https://newrelic.com/infrastructure/</url>
  </organization>
  <dependencies>
    <dependency>
      <groupId>org.yaml</groupId>
      <artifactId>snakeyaml</artifactId>
      <version>1.13</version>
    </dependency>
    <dependency>
      <groupId>commons-cli</groupId>
      <artifactId>commons-cli</artifactId>
      <version>1.4</version>
    </dependency>
    <dependency>
      <groupId>com.google.code.gson</groupId>
      <artifactId>gson</artifactId>
      <version>2.8.0</version>
    </dependency>
  </dependencies>
  <build>
    <sourceDirectory>src</sourceDirectory>
    <resources>
      <resource>
        <directory>src</directory>
        <excludes>
          <exclude>**/*.java</exclude>
        </excludes>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <configuration>
          <archive>
            <manifest>
              <mainClass>org.newrelic.nrjmx.Application</mainClass>
            </manifest>
          </archive>
          <descriptorRefs>
            <descriptorRef>jar-with-dependencies</descriptorRef>
          </descriptorRefs>
        </configuration>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.coderplus.maven.plugins</groupId>
        <artifactId>copy-rename-maven-plugin</artifactId>
        <version>1.0</version>
        <executions>
          <execution>
            <id>copy-file</id>
            <phase>package</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <sourceFile>target/${project.artifactId}-${project.version}-jar-with-dependencies.jar</sourceFile>
              <destinationFile>bin/nrjmx.jar</destinationFile>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>deb</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <plugins>
          <!-- DEB definitions for NR-JMX -->
          <plugin>
            <artifactId>jdeb</artifactId>
            <groupId>org.vafer</groupId>
            <version>1.5</version>
            <executions>
              <execution>
                <phase>package</phase>
                <goals>
                  <goal>jdeb</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <dataSet>
                <data>
                  <type>file</type>
                  <src>target/${project.artifactId}-${project.version}-jar-with-dependencies.jar</src>
                  <dst>/usr/lib/${project.artifactId}/${project.artifactId}-${project.version}-jar-with-dependencies.jar</dst>
                </data>
                <data>
                  <type>link</type>
                  <linkName>/usr/lib/${project.artifactId}/${project.artifactId}.jar</linkName>
                  <linkTarget>/usr/lib/${project.artifactId}/${project.artifactId}-${project.version}-jar-with-dependencies.jar</linkTarget>
                  <symlink>true</symlink>
                </data>
                <data>
                  <type>file</type>
                  <src>bin/${project.artifactId}</src>
                  <dst>/usr/bin/${project.artifactId}</dst>
                  <mapper>
                    <type>perm</type>
                    <filemode>755</filemode>
                  </mapper>
                </data>
                <data>
                  <type>file</type>
                  <src>README.md</src>
                  <dst>/usr/share/doc/${project.artifactId}/README</dst>
                </data>
                <data>
                  <type>file</type>
                  <src>LICENSE</src>
                  <dst>/usr/share/doc/${project.artifactId}/LICENSE</dst>
                </data>
              </dataSet>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>rpm</id>
      <activation>
          <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <plugins>
            <!-- RPM definitions for NR-JMX tool -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>rpm-maven-plugin</artifactId>
                <version>2.1.5</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>rpm</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <license>https://newrelic.com/terms (also see LICENSE installed with this package)</license>
                    <packager>New Relic Infrastructure Team &lt;infrastructure-eng@newrelic.com&gt;</packager>
                    <group>Application/System</group>
                    <sourceEncoding>utf-8</sourceEncoding>
                    <targetOS>x86_64-linux</targetOS>
                    <needarch>x86_64</needarch>
                    <mappings>
                        <mapping>
                            <directory>/usr/lib/${project.artifactId}</directory>
                            <sources>
                                <source>
                                    <location>target/${project.artifactId}-${project.version}-jar-with-dependencies.jar</location>
                                </source>
                                <softlinkSource>
                                    <location>/usr/lib/${project.artifactId}/${project.artifactId}-${project.version}-jar-with-dependencies.jar</location>
                                    <destination>${project.artifactId}.jar</destination>
                                </softlinkSource>
                            </sources>
                        </mapping>
                        <mapping>
                            <directory>/usr/bin</directory>
                            <username>root</username>
                            <groupname>root</groupname>
                            <configuration>false</configuration>
                            <directoryIncluded>false</directoryIncluded>
                            <filemode>755</filemode>
                            <sources>
                                <source>
                                    <location>bin/nrjmx</location>
                                </source>
                            </sources>
                        </mapping>
                        <mapping>
                            <directory>/usr/share/doc/${project.artifactId}</directory>
                            <documentation>true</documentation>
                            <username>root</username>
                            <groupname>root</groupname>
                            <!-- <filemode>444</filemode> -->
                            <sources>
                                <source>
                                    <location>README.md</location>
                                </source>
                                <source>
                                    <location>LICENSE</location>
                                </source>
                            </sources>
                        </mapping>
                    </mappings>
                </configuration>
            </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
  <properties>
    <maven.compiler.source>1.7</maven.compiler.source>
    <maven.compiler.target>1.7</maven.compiler.target>
    <maven.compiler.compilerVersion>1.7</maven.compiler.compilerVersion>
  </properties>

</project>
